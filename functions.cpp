/*
 * This is a simple program that determines the string length of a command line parameter in C++.
 * Note the header structure and files.
 * The only thing included here is functions.h
 * functions.h contains all other directives 
 */
/* 
 * File:   functions.cpp
 * Author: Ahmed Eid
 */
#include "functions.h"
int string_length(char *str){

    return strlen(str);
}